const colors1 = ["periwinkle", "sand brown", "salmon", "coral", "seagreen", "midnight blue"]

//   DEFINING       CHECKING               CHANGING
for (let index = 0; index < colors1.length; index += 1) {
    const currentColor = colors1[index]
    console.log(`Color number ${index} is ${currentColor}!`)
}